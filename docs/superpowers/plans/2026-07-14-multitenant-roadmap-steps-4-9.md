# Multi-Tenant Migration — Roadmap for Steps 4–9

Steps 1–3 are DONE + verified (auth core; user_id migration + write-path; read-scoping + route-auth). Branch `feat/multitenant`, 919 tests green, mechanical scoping-audit gate in place. This roadmap scopes the remaining steps; each gets its own detailed bite-sized plan written just-in-time (grounded in the then-current code) and executed via subagent-driven-development (Sonnet implementers + adversarial review per task, live-verify where applicable). Executed autonomously without stopping; operator reviews at the end.

## Step 4 — Frontend: route groups + AppShell + onboarding routing
**Goal:** Session-aware UI. **Scope:** introduce `(auth)` vs `(app)` Next route groups so login/register render chrome-free (relocates the 8 client pages under `(app)`); `src/app/page.tsx` becomes session-aware (login → onboarding-if-no-profile → feed); build login + register pages (compose existing primitives — **UI canon: compose the 13 primitives + tokens, mirror Storybook, never invent components**); `AppShell` ProfileChip shows the real user + a logout affordance; a conditional **Admin** sidebar group when `role==='admin'`; wire `__resetChecksStore` to logout/user-change; **onboarding routing** — a profile-less session (ProfileMissingError) routes to an onboarding page that calls `PUT /api/profile` (the upsert from Step 3), instead of 500ing at the feed. Storybook stories for new compositions. **Key risk:** the feed/API throws ProfileMissingError for fresh registrants — the shell must catch/redirect. **Deps:** Step 1 (getSession/session route), Step 3 (profile upsert).

## Step 5 — Résumé file storage re-root
**Goal:** survive local-disk-now → VPS-later. **Scope:** replace `join(process.cwd(),'data','uploads')` with `CALIBER_UPLOADS_DIR` (fail-loud env like DATABASE_URL; fixes the `next standalone` cwd bug); per-user directories `{userId}/resumes/{hash}.{ext}`; store **relative** keys in `resumes.originalPath`/`tailored_resumes.pdfPath`, resolve against root at read time; a one-time migration of existing rows + files; any file-serving route is `requireUser`-guarded with an ownership check (never a public static mount). **Deps:** Step 2/3 (per-user rows). **Note:** S3 stays out of scope (layout maps 1:1 to an S3 key scheme later).

## Step 6 — Admin routes + page
**Goal:** admin surface. **Scope:** `GET /api/admin/users` (list + per-user counts, never the hash) guarded by `requireAdmin()`; **decision #7 (operator-locked, against recommendation): admin has FULL content access** to users' résumés/jobs/applications — implement by `requireAdmin()` routes that pass the **target** userId into the SAME scoped repos (so no unscoped query is ever added); admin page composing the existing Tracker/console Card table treatment; `PATCH /api/sources/:id` becomes `requireAdmin()` (the deferred admin-gap TODO); conditional Admin sidebar group already added in Step 4; Storybook stories. **Risk to note (not re-litigate):** full content access to career/salary data — flagged, operator-chosen. **Deps:** Steps 1 (requireAdmin), 3 (scoped repos), 4 (shell).

## Step 7 — Test hardening
**Goal:** close test gaps. **Scope:** much is already done (route tests mock sessions; second-user isolation tests exist; mechanical audit gate). Remaining: a shared `TEST_USER_ID`/second-user fixture consolidation if duplicated; e2e `globalSetup.ts` seeds admin + two users with Playwright `storageState` login (env-gated — the deferred Playwright E2E from the backlog); confirm the full `npm run check` (typecheck + vitest + contract:check + build) is green and add any missing coverage the prior steps flagged. **Deps:** all prior.

## Step 8 — Contabo VPS deploy artifacts  ⚠️ operator-run
**Goal:** deploy-ready, not deployed (I have no VPS access/secrets). **Scope:** `Dockerfile` (Playwright/Chromium base or `npx playwright install --with-deps chromium`; `next standalone` output + copied `.next/static` + `public/`), `docker-compose.yml` (app + `postgres:17` localhost-only volume-backed + `caddy` auto-TLS), `Caddyfile` (SSE-clean: buffering off, read timeouts > SSE idle + ~36s scoreMatch, `client_max_body_size ≥ 10MB`), **exactly one app process** (no PM2 cluster — in-memory SSE registry + single worker), uploads bind-mount matching `CALIBER_UPLOADS_DIR`, `env_file` template (never secrets in args), nightly `pg_dump` + uploads `rsync` runbook, and a `DEPLOY.md` runbook (build, migrate, seed admin, data move: pg_dump/restore + rsync — cheap because keys are relative). **The operator runs the actual deploy** (SSH, secrets, DNS). **Deps:** Steps 5 (uploads root), 1–3 (single-process constraint documented).

## Step 9 — Docs reconciliation (CLAUDE.md docs-are-canon)
**Goal:** authoritative docs match reality. **Scope:** `api-contract.md` (§2 ErrorCode dump refresh incl. UNAUTHORIZED/FORBIDDEN + the 4 auth schemas — the known follow-up; per-user route semantics; admin routes); `system-architecture.md` §1/§6 (users/sessions tables, profile no longer singleton, per-user cost-cap stance stays global, in-memory registry single-process constraint, uploads re-root); README reconciliations; `component-inventory.md` (SidebarNav admin group, account/logout affordance, auth pages). **Deps:** all prior (reconcile last so docs reflect final state).

## Cross-cutting carry-forwards (from Steps 1–3 reviews)
- `recompute-eligibility.ts` CLI needs per-owner-profile resolution (KNOWN-FOLLOWUP) — address in Step 6 or 7.
- Pre-public-VPS tripwires (NOT blockers, note in Step 8/9 docs): open-registration invite gate (decision #3), no session expiry (decision #4), login timing equalization, per-user cost cap, `sessions.userId` index, timestamptz migration.
- Merge conflict with `feat/parallel-scoring` expected at url_checks/worker (decision #1 revised — url_checks scoped here).

## Execution order
4 → 5 → 6 → 7 → 8 (artifacts) → 9. Each: just-in-time detailed plan, subagent-driven build + review, green gate, live-verify (4/5/6), update ledger + memory. No stopping between steps; final consolidated report at the end.
